---
layout: default
title: Index Page
---

- site.collated_posts.sort.reverse.each do |year, months|
	%h2= year
	- months.sort.reverse.each do |month,days|
		%ol
			- days.sort.reverse.each do |day,posts|
				- posts.each do |post|
					%li.postlist
						%strong= "[#{post.date.strftime("%b %d")}]"
						= link_to(h(post.title), post.url)
		%hr